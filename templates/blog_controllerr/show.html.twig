<!DOCTYPE html>

<title>Blog</title>

{% block body %}
    <h1>Blog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blog.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ blog.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ blog.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('blog_controllerr_index') }}">back to list</a>

    <a href="{{ path('blog_controllerr_edit', {'id': blog.id}) }}">edit</a>

    {{ include('blog_controllerr/delete.html.twig') }}
{% endblock %}
