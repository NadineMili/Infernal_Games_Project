{% extends 'backBase.html.twig' %}
{% block body %}


    <h2>CategorieActivite</h2>
    <br>
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <div class="row">
                    </div>

                    <div class="tableau">
                        <table  class="table" >
                            <thead>
                            <tr>
                                <th class="card-header bg-danger text-white">Id</th>
                                <th class="card-header bg-danger text-white">Label</th>
                                <th class="card-header bg-danger text-white">More</th>
                            </thead>
                            </tbody>
                            </tr>
                            <tr>
                                <td>{{ category.id }}</td>
                                <td>{{ category.label }}</td>
                                <td>
                                    <a class="btn btn-light" href="{{ path('category_activity_edit', {'id': category.id}) }}" >edit</a>

                                    <a  class="btn btn-dark">{{ include('category_activity/_delete_form.html.twig') }}</a></td>
                            </tr>
                        </table>

                    </div>
                </div>
            </div>
        </div>
    </div>
    <div align="center">
        <a href="{{ path('category_activity_index') }}" class="primary-btn">back to categories</a>
    </div>
{% endblock %}